# Frontend env template — copy to .env and fill values
# All Gemini calls go through the backend proxy (/api/gemini/generate),
# so the API key lives only in backend-cdb/.env.

VITE_API_URL=http://localhost:2001/api
VITE_SOCKET_URL=http://localhost:2001
