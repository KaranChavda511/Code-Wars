# Backend env template — copy to .env and fill values
# Atlas project: ecommerce-backend (Cluster0) → DB "test"
# Existing collections: challenges, multiplayersessions, submissions, users
# (project name is misleading; this cluster actually hosts Code-Wars data)

PORT=
NODE_ENV=
MONGO_URI=
JWT_SECRET=
